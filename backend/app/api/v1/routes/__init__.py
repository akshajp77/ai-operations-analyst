"""Route modules, one per resource.

One file per resource keeps routes findable and keeps merge conflicts
localised — two engineers adding endpoints to different resources never touch
the same file.
"""
